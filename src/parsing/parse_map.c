#include "parsing.h"

// int	parse_map(t_parse *parse)
// {
// 	parse->curent_data = parse->curent_data->next;
// 	while (parse->curent_data && parse->curent_data->content[0] == '\n')
// 		parse->curent_data = parse->curent_data->next;
// 	while(parse->curent_data)
// 	{

// 		parse->curent_data = parse->curent_data->next;
// 	}
// 	return (1);
// }
